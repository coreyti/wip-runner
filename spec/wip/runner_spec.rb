require 'spec_helper'

describe WIP::Runner do
  it 'has a version number' do
    expect(WIP::Runner::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
